@*@model IEnumerable<Doska.Models.Ads>*@
@model AdsesListViewModel



@{ ViewData["Title"] = "Показать обьявления"; }


<div class="doska-form ml-5 mr-5">

    <h4>
        @Model.Catalog.NameTitle/@Model.Catalog.NameSubtitle

    </h4>


    <table class="table table-striped table-hover table-sm table-bordered">
        <thead>
            <tr>
                <th>
                    Id
                </th>
                <th>
                    Дата
                </th>
                <th>
                    Текст
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Adses)
            {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Id)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.AdsCreate)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.AdsText)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
    </td>
</tr>}
        </tbody>
    </table>


    <ul page-model="@Model.PagingInfo" page-controller="Home" page-action="ViewSelectAds" page-classes-enabled="true"
        page-class-item="page-item" page-class-link="page-link" page-class-pagination="pagination"
        page-class-normal="" page-class-selected="active" class="pagination">  </ul>


    @*<ul class="pagination">
            <li class="page-item">
                <a class="page-link" asp-controller="Home" asp-action="ViewSelectAds">Previous</a>
            </li>

            @for (int i = 1; i < 10; i++)
            {
        <li class="page-item">
            <a class="page-link" asp-controller="Home" asp-action="ViewSelectAds" asp-route-contentid="1" asp-route-pageid="@i">@i</a>
        </li>
        }
            <li class="page-item">
                <a class="page-link" asp-controller="Home" asp-action="ViewSelectAds">Next</a>
            </li>

        </ul>*@
    <div>
        <a asp-controller="Home" asp-action="Index">На главную</a>
    </div>
</div>
